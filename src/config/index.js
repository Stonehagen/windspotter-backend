const dataValues = ['t', 'v', 'u'];
const fCModel = 'regular-lat-lon_model';
const fCHeight = '_65_';
const serverDataTimeDelay = 5 * 60 * 1000;

module.exports = {
  dataValues,
  fCModel,
  fCHeight,
  serverDataTimeDelay,
};
